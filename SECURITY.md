# Security

The repository intentionally contains attack-like text fixtures. They are synthetic and should not include real secrets, credentials, endpoints, or private data.

When adding cases:

- use placeholders only
- keep expected findings explicit
- avoid network-dependent tests
- add regression coverage for every new detector
